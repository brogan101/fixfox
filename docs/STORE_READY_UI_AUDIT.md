# Store Ready UI Audit (2026-03-04)

## Runtime audit
- Command: `.\\.venv\\Scripts\\python.exe -m src.tests.smoke`
  - Result: **pass**.
- Command: offscreen startup check (`AppShell` for ~1.2s)
  - Result: **pass** (`UI_STARTUP_OK`).
  - QFont warnings observed: **none**.
- Command: `.\\.venv\\Scripts\\python.exe scripts/capability_audit.py`
  - Result: **pass** (`140 ok / 0 failing`).

## UI walk-through inventory and repair summary

### Home
- Verified controls: quick check CTA, goal cards, favorites actions, session list actions.
- Issues found: dense spacing at lower densities.
- Fixes applied:
  - unified row/card spacing through shared style helpers (`src/ui/style/style_guide.py`, `src/ui/widgets.py`, `src/ui/components/rows.py`).

### Playbooks
- Verified controls: search, segmented tools/runbooks, script task actions, runbook actions, fast file index controls.
- Issues found: Basic/Pro separation needed clear policy and consistent visibility behavior.
- Fixes applied:
  - mode visibility continues to use central policy (`src/ui/ui_state.py` + `MainWindow._apply_*_mode_visibility`).
  - capability audit repaired tool entrypoint integrity so Playbooks tool capabilities are no longer marked broken.

### Diagnose
- Verified controls: search, severity filter, recommended toggle, grouped findings, next action.
- Issues found: row/toolbar rhythm mismatch vs other pages.
- Fixes applied:
  - row/header spacing normalized by shared spacing tokens and QSS tab/header row rules.

### Fixes
- Verified controls: scope/risk filters, preview/run, rollback list actions.
- Issues found: filter/row density differences.
- Fixes applied:
  - shared row spacing + QSS list/tree row sizing improves consistency.

### Reports
- Verified controls: preset/masking/log options, evidence list, generate/validate actions.
- Issues found: control and tab styling drift vs rest of shell.
- Fixes applied:
  - global QSS tab styling + header styling unified with main shell tokens.

### History
- Verified controls: session search/reopen/re-export/compare, run center list.
- Issues found: visual consistency with list rows.
- Fixes applied:
  - shared row spacing and density-driven row sizing.

### Settings
- Verified controls: search, safety/privacy/appearance toggles, advanced DB maintenance actions.
- Issues found: needed stronger consistency with top-level shell spacing.
- Fixes applied:
  - style token spacing used consistently across cards/buttons/rows.

## Functionality audit outcome
- Capability matrix now generated by automation (`scripts/capability_audit.py`) into `docs/FUNCTIONALITY_MATRIX.md`.
- Core capability groups (tools/fixes/script tasks/runbooks/exports/masking/validator/evidence) show **working** health.

## Root functionality issues fixed during this pass
1. Capability registry tool entrypoint mismatch:
   - Issue: tool capabilities referenced `ui.main_window.launch_tool`, but no such callable existed.
   - Fix:
     - added `core.toolbox.launch_tool(...)` and `tool_map()` in `src/core/toolbox.py`.
     - updated tool capability entrypoints in `src/core/registry.py` to `core.toolbox.launch_tool:<tool_id>`.

2. Script audit import robustness:
   - Issue: running `scripts/capability_audit.py` directly failed with `ModuleNotFoundError: src`.
   - Fix: added repo-root `sys.path` bootstrap in `scripts/capability_audit.py`.

3. Top bar run status polish:
   - Issue: status detail ellipsis could look clipped after resize.
   - Fix: retained raw detail text and re-ellipsize on resize (`src/ui/main_window.py`).

## Theme / style consistency audit
- Hardcoded page-level stylesheet overrides: **none found**.
- Global stylesheet source: `src/ui/app_qss.py`.
- Theme token source: `src/ui/theme.py`.
- Added/verified:
  - themed scrollbars,
  - splitter styling,
  - tree header/item and tab styling,
  - density-consistent spacing usage via `src/ui/style/style_guide.py`.

## Repo hygiene audit
- Removed tracked temporary artifacts from git index:
  - `.smoke_tmp/**`
  - `.unit_tmp/**`
- `.gitignore` already covered temp/build/python cache paths.

## Release decision
- **Go** for python-first release candidate based on current QA gates:
  - smoke pass,
  - startup pass with no QFont warning spam,
  - capability harness pass,
  - no known dead capability entrypoints in registry.
